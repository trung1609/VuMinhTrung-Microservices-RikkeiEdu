spring:
  application:
    name: config-server-service
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  cloud:
    config:
      server:
        git:
          uri: https://github.com/trung1609/Logistic-Config.git
          default-label: main
          username: trung1609
          password: PASSWORD_ACCESS_TOKEN
    bus:
      enabled: true
server:
  port: 8888

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8686/eureka/
    fetch-registry: true
    register-with-eureka: true